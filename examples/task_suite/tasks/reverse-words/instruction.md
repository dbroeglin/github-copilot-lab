This project has a failing test. Implement the single function in `strtools.py` so that
`python -m pytest -q /tests/test_strtools.py` passes.

Read `test_strtools.py` first to learn the exact expected behavior. Do not modify the tests.
