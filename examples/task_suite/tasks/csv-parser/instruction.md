This project has failing tests. Implement `parse_csv` in `csvtools.py` so that
`python -m pytest -q /tests/test_csvtools.py` passes.

The tests in `test_csvtools.py` are the exact spec, including quoting, escaped quotes,
embedded commas/newlines, and CRLF handling. Read them carefully first. Do not modify the tests.
