name: {{project_name}}
version: 1.0.0
description: GitHub Copilot research experiments.

dependencies:
  apm:
    # Add reusable agent context here, e.g.:
    # - github/awesome-copilot/agents/api-architect.agent.md
  mcp:
    # MCP servers installed into every detected client, e.g.:
    # - name: io.github.github/github-mcp-server
    #   transport: http
