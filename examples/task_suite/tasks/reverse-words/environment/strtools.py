"""A single tiny string helper.

The one function below is intentionally left unimplemented. The accompanying tests in
``test_strtools.py`` describe exactly how it should behave. Implement it so the suite passes.
"""


def reverse_words(text):
    """Return ``text`` with its whitespace-separated words in reverse order.

    Runs of whitespace are treated as a single separator and the result is joined with
    single spaces, so ``"  a   b "`` becomes ``"b a"``. Empty or whitespace-only input
    returns an empty string.
    """
    raise NotImplementedError
